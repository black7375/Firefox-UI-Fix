on:
  push:
    branches:
      - master
      - photon-style
  pull_request:
    branches:
     - master
name: 'ShellCheck'

jobs:
  shellcheck:
    name: Shellcheck
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run ShellCheck
      uses: ludeeus/action-shellcheck@master
